{
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp style.css dist/ || copy index.html dist && copy style.css dist"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "firebase-tools": "^15.16.0"
  }
}
